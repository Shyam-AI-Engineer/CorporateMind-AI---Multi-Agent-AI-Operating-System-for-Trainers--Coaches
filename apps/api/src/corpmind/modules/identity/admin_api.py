"""Admin-only identity routes (PlatformAdmin role required)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()

# Admin routes are scaffolded here; implementations added in Phase 1.
# Every route must use the @platform_admin_only decorator (not yet implemented).
